

namespace Guitar_World {
    
    /// <summary> Pickups describes what set of pickups a guitar has where S =
    /// single coil and H = humbucker </summary>
    public enum PickupSet {
        SSS,
        SSH,
        SS,
        SH,
        HH,
        HHH,
    }
}