

namespace Guitar_World {
    /// <summary> This is a list of the possible tunnings of each string and this has been
    /// been fitted to standard tuning (EADGBE), but it can be extended if needed </summary>
    public enum String {
        E_deep,
        A,
        D,
        G,
        B,
        E_high,
    }
}