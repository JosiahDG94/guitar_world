

namespace Guitar_World {
    /// <summary> This is the kinds of tremolo systems that are installed on
    /// the guitar. The tremolo-kind is the standard one for stratocasters </summary>
    public enum TremoloSystem {
        Tremolo,
        Bigsby,
        None,
    }
}